server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # react-router uses client-side routes (e.g. /admin) - fall back
    # to index.html so a hard refresh on those doesn't 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
