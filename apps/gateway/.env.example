PORT=3000
FRONTEND_ORIGIN=http://localhost:5173

MONGODB_URI=mongodb://localhost:27017/vitalens

GEO_SERVICE_URL=http://localhost:8000

JWT_SECRET=change-me-to-a-long-random-string

# Admin account (single account, no public user accounts - see stack table).
# Generate the hash with: node -e "console.log(require('bcrypt').hashSync('yourpassword', 10))"
ADMIN_EMAIL=admin@vitalens.local
ADMIN_PASSWORD_HASH=
